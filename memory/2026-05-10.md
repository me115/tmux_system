- Installed OpenSpec Codex global skills into `/Users/openclaw/.codex/skills` using the local `@fission-ai/openspec` CLI package version 1.2.0. Generated 11 skills: `openspec-propose`, `openspec-explore`, `openspec-new-change`, `openspec-continue-change`, `openspec-apply-change`, `openspec-ff-change`, `openspec-sync-specs`, `openspec-archive-change`, `openspec-bulk-archive-change`, `openspec-verify-change`, and `openspec-onboard`.
- Created OpenSpec change `add-tmux-ai-task-manager` for a local CLI task manager that controls tmux/Codex task windows, tracks statuses in SQLite, promotes WAIT tasks, and handles Codex turn-complete notifications. Artifacts are complete and valid.
- Configured OpenSpec artifact generation to default to Simplified Chinese. Global default is implemented via user schema override at `/Users/openclaw/.local/share/openspec/schemas/spec-driven`; this project also has `openspec/config.yaml` with Chinese context/rules while preserving OpenSpec machine-parsed markers.
- Implemented `add-tmux-ai-task-manager`: repo-local Python `aitask` CLI with SQLite storage, tmux window/runtime integration, Codex notify handler, optional desktop/Feishu notifications, smoke tests, docs, and PATH symlinks in `/Users/openclaw/.openclaw/bin`. OpenSpec apply progress is 35/35 complete. Updated `/Users/openclaw/.codex/config.toml` to use `bin/codex-notify-dispatch`, preserving the existing Computer Use turn-ended notify and adding `aitask-codex-notify`.
- Created `/Users/openclaw/.tmux.conf` with `Ctrl-a` as tmux prefix. Pane shortcuts: `Ctrl-a v` split left/right, `Ctrl-a s` split top/bottom, `Ctrl-a ;` left pane, `Ctrl-a '` right pane, `Ctrl-a [` up pane, `Ctrl-a ]` down pane. Updated `tmux.md` and added `/Users/openclaw/.openclaw/bin/thelp`.
- Added `aitask` zsh aliases in `/Users/openclaw/.zshrc`: `at`, `atn`, `atl`, `atwait`, `ato`, `atw`, `atr`, `atd`, `ate`, `ats`, `atdoc`, `atcfg`. Updated `thelp`, `docs/aitask.md`, and `tmux.md`. Removed a self-appending PATH line from `.zshrc`.
- Corrected aitask state semantics: viewing/selecting a tmux task window must not auto-convert `WAIT` to `RUNNING`. Removed active tmux focus hooks and changed `aitask open`/`ato` to navigation-only. `RUNNING` now means the user explicitly resumed model work via `atr <id>` or task creation. Marked task #1 back to `WAIT`.
- Added automatic Codex-running detection based on task pane output: `aitask sync` enables `tmux pipe-pane` on the Codex main pane, and hidden `aitask _tmux-pane-monitor <task_id> <pane_id>` changes `WAIT/PENDING` to `RUNNING` when Codex TUI outputs `Working` / `esc to interrupt`. Existing tasks #1 and #2 are WAIT with their Codex panes monitored.
- Updated `aitask new` default command to `codex --dangerously-bypass-approvals-and-sandbox`, per user preference for bypassing approvals and sandbox by default. Task list timestamps now parse ISO timestamps and display them in the machine's local timezone, so prior UTC records show as local time.
- Added `td='tmux detach'` alias to `/Users/openclaw/.zshrc` and documented it in `thelp`, `tmux.md`, and `docs/aitask.md`.
- Updated `aitask list` / `sync` semantics: active tasks whose tmux windows no longer exist are marked `DONE` and hidden from the default list; `--all` can still show them for history.
- Fixed WAIT-to-RUNNING detection for Codex TUI status lines that redraw without newline: `_tmux-pane-monitor` now reads byte chunks, and `aitask list` / `sync` use `capture-pane` as a fallback. Task #6 was auto-corrected to RUNNING.
- Added open-time Feishu forwarding: `aitask open --feishu <id>` captures the task's main pane and sends it via OpenClaw (`AITASK_FEISHU_TARGET`) or webhook before opening tmux. Updated `ato` alias to `aitask open --feishu`.
- Added Feishu bot interactive control for `aitask` through the live `openclaw-lark` handler, with sender allowlist checks. Supported commands include `at`, `at wait`, `at new`, `at show <id>`, `at tail <id>`, `at open <id>`, `at send <id> <text>`, and shortcut `at <id> <text>`. `at open` activates the local tmux window without attaching the gateway process and returns recent Codex pane output to Feishu.
- Fixed `aitask send`: it now accepts Codex trust prompts before submitting text, uses tmux paste-buffer for Unicode input, and only marks a task RUNNING after the pane shows Codex actually working. Re-sent task #8's requested command successfully.
- Fixed stale RUNNING after Codex turn completion: running detection now uses recent pane output instead of historical scrollback, pipe monitor clears consumed `Working` markers, and sync can mark a completed/idle task back to WAIT. Task #8 was corrected to WAIT.
- Configured OpenClaw Feishu multi-bot routing so `codex_runner` keeps its original Feishu app/config and the new `aitask` Feishu app is added as account/agent/binding `aitask`, with workspace `/Users/Shared/openclaw-share/repos`; validation, gateway restart, health, channel status, and logs confirmed `feishu[aitask]` is running and dispatching to `agent:aitask`. Added the new app-specific sender id seen in logs to `aitask.allowFrom` only, so `at` commands can pass the aitask command allowlist without changing `codex_runner`.
- Added RUNNING-to-WAIT Feishu notifications for `aitask`: all status transitions now share a helper that sends a WAIT notification only when the previous state was `RUNNING`; this covers Codex notify, `aitask sync` idle correction, manual `atw`, and `send` correction paths without duplicate sends. Configured `/Users/openclaw/.config/aitask/config.json` to use OpenClaw Feishu account `aitask` and the current bot p2p user target.
